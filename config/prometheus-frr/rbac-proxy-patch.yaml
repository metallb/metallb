# Native TLS is now built into the controller, speaker, and frr-metrics binaries.
# kube-rbac-proxy sidecars are no longer needed.
# This file is kept for kustomize compatibility but applies no changes.
