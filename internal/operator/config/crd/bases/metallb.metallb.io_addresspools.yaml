
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: addresspools.metallb.metallb.io
spec:
  group: metallb.metallb.io
  names:
    kind: AddressPools
    listKind: AddressPoolsList
    plural: addresspools
    singular: addresspools
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: AddressPools is the Schema for the addresspools API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AddressPoolsSpec defines the desired state of AddressPools
          properties:
            address-pools:
              items:
                properties:
                  addresses:
                    description: A list of IP address ranges over which MetalLB has
                      authority. You can list multiple ranges in a single pool, they
                      will all share the same settings. Each range can be either a
                      CIDR prefix, or an explicit start-end range of IPs.
                    items:
                      type: string
                    type: array
                  auto-assign:
                    description: auto-assign flag used to prevent MetallB from automatic
                      allocation for a pool.
                    type: boolean
                  avoid-buggy-ips:
                    description: Avoid buggy ips is used to mark .0 and .255 as unusable.
                    type: boolean
                  bgp-advertisements:
                    description: BGP Advertisement allow user to customize BGP advertisements,
                      by default BGP advertise /32 with localpref set to 0 and no
                      BGP communities.
                    items:
                      properties:
                        aggregation-length:
                          description: The aggregation-length advertisement option
                            lets you “roll up” the /32s into a larger prefix.
                          minimum: 1
                          type: integer
                        communities:
                          description: BGP communities
                          items:
                            type: string
                          type: array
                        localpref:
                          description: BGP LOCAL_PREF attribute which is used by BGP
                            best path algorithm, Path with higher localpref is preferred
                            over one with lower localpref.
                          format: int32
                          type: integer
                      type: object
                    type: array
                  name:
                    description: Address Pool Name
                    type: string
                  protocol:
                    description: Protocol can be used to select how the announcement
                      is done,
                    enum:
                    - layer2
                    - bgp
                    type: string
                required:
                - addresses
                - name
                - protocol
                type: object
              type: array
          required:
          - address-pools
          type: object
        status:
          description: AddressPoolsStatus defines the observed state of AddressPools
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
