# permissions for end users to view peers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: peers-viewer-role
rules:
- apiGroups:
  - metallb.metallb.io
  resources:
  - peers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - metallb.metallb.io
  resources:
  - peers/status
  verbs:
  - get
