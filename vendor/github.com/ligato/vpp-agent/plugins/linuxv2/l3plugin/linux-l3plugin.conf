# Used to disable linux l3plugin. Turned off by default.
disabled: false

# How many go routines (at most) will split configured network namespaces to execute
# the Dump operation in parallel.
dump-go-routines-count: 10
