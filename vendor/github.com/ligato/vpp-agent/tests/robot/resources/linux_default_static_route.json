{
  "name": "${routename}",
  "default": ${isdefault},
  "namespace": {
      "name": "${namespace}",
      "type": 1
  },
  "interface": "${interface}",
  "gw_addr": "${next_hop}",
  "metric": ${metric}
}
