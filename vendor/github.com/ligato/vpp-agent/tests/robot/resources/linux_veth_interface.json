{
  "enabled": ${enabled},
  "mtu": ${mtu},
  "ip_addresses": [
    "${ip}/${prefix}"
  ],
  "namespace": {
      "reference": "${namespace}",
      "type": 1
  },
  "name": "${name}",
  "host_if_name": "${host_if_name}",
  "phys_address": "${mac}",
  "type": "VETH",
  "veth": {
    "peer_if_name": "${peer}"
  }
}
