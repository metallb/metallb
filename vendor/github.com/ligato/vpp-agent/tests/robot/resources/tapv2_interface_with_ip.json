{
  "name": "${name}",
  "type": "TAP",
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "vrf": ${vrf},
  "ip_addresses": [
    "${ip}/${prefix}"
  ],
  "tap": {
    "version": 2,
    "host_if_name": "${host_if_name}"
  }
}
