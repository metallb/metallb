{
  "name": "${name}",
  "type": "MEMIF",
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "vrf": ${vrf},
  "memif": {
    "master": ${master},
    "id": ${id},
    "socket_filename": "${socket}"
  }
}
