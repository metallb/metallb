{
  "name": "${name}",
  "type": "VETH",
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "veth": {
    "peer_if_name": "${peer}"
  },
  "namespace": {
      "reference": "${namespace}",
      "type": 4
  },
  "mtu": ${mtu},
  "ip_addresses": [
    "${ip}/${prefix}"
  ]
}