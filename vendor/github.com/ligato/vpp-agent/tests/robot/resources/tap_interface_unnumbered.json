{
  "name": "${name}",
  "type": 3,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "unnumbered": {
    "IsUnnumbered": ${unnumbered},
    "InterfaceWithIp": "${interface_with_ip_name}"
  },
  "tap": {
    "host_if_name": "${host_if_name}"
  }
}
