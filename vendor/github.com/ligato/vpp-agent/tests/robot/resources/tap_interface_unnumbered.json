{
  "name": "${name}",
  "type": "TAP",
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "mtu": ${mtu},
  "unnumbered": {
    "interface_with_ip": "${interface_with_ip_name}"
  },
  "tap": {
    "host_if_name": "${host_if_name}"
  }
}
