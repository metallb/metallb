{
  "name": "${name}",
  "type": 0,
  "enabled": ${enabled},
  "phys_address": "${mac}",
  "veth": {
    "peer_if_name": "${peer}"
  },
  "mtu": ${mtu},
  "vrf": ${vrf},
  "ip_addresses": [
    "${ip}/${prefix}"
  ]
}
